{
  "name": "o5-stack-mono",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write .",
    "check": "turbo typecheck",
    "typegen": "turbo typegen",
    "test": "turbo test",
    "prepare": "husky",
    "postinstall": "./scripts/setup-agents.sh",
    "ralph": "./scripts/backlog/codex-afk.sh 50",
    "audit:health": "bun x fallow health --complexity",
    "audit:dupes": "bun x fallow dupes",
    "audit:prune": "bun x fallow dead-code",
    "audit:fix": "bun x fallow fix && bun run format"
  },
  "devDependencies": {
    "@testing-library/react": "^16.3.2",
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "prettier": "^3.8.3",
    "prettier-plugin-organize-imports": "^4.3.0",
    "prettier-plugin-tailwindcss": "^0.8.0",
    "turbo": "^2.9.12",
    "typescript": "6.0.3"
  },
  "packageManager": "bun@1.3.0",
  "engines": {
    "node": ">=20"
  }
}
